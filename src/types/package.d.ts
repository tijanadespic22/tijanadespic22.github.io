type Package = {
  title: string;
  subtitle: string;
  price?: string;
  promoPrice?: string;
  priceNote?: string;
  features: string[];
  cta: string;
  highlighted?: boolean;
  iconName?: IconName;
};
